CREATE TABLE [Provider].[Provider] (
    ProviderID UNIQUEIDENTIFIER PRIMARY KEY DEFAULT NEWID(),

    -- TODO: add fields for provider identification
    --FirstName NVARCHAR(50),
    --LastName NVARCHAR(50),
    --Specialization NVARCHAR(100)
);

